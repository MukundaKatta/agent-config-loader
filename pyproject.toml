[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-config-loader"
version = "0.1.0"
description = "Load and validate AI agent configuration from environment variables"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7", "ruff>=0.4"]

[tool.hatch.build.targets.wheel]
packages = ["src/agent_config_loader"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
